apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: deployment-demo
spec:
  selector:
    app: web-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
